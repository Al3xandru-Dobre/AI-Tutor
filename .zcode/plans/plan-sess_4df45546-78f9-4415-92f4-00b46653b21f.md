# Execution Plan (updated per your instruction: 2 subagents for Part 1, I take Part 2)

## Status
- Two background agents are already launched with disjoint file sets:
  - **Agent A (archivist)**: creates `docs/{history,guides,plans}`, moves ~31 historical MDs unchanged to `docs/history/`, rebuilds `CHANGELOG.md` (folding in CHANGELOG_V3.2.0.md), enforces root invariant = `README.md, CHANGELOG.md, CONTRIBUTING.md, ROADMAP.md` (+LICENSE).
  - **Agent B (editor)**: writes 9 simplified docs (`docs/guides/{user-guide,deployment-docker,redis,chromadb,notebook,model-providers}.md`, `docs/architecture.md`, `docs/plans/{auth,notebook-roadmap}.md` — each 50–70% shorter), rewrites `README.md` as a ~300-line hub with a Documentation index linking everything, moves its 11 source docs to `docs/history/`, and fixes cross-references in root files/scripts.
- Plan mode blocked their writes — after approval I collect their state (resume via SendMessage if stalled) and re-dispatch if needed, then verify.

## My work — Part 2: `docs/plans/visual-redesign.md`
Direction confirmed with you: **Japanese-inspired professional ("Washi & Ink")**, scope = design system + light HTML cleanup (no framework/build step, HTML structure preserved).

The plan document will include, grounded in the code audit I just completed:

1. **Audit findings with file:line evidence** — including the newly found active bug: `light-theme.css:46` `body.dark-theme` overrides `style.css :root` (higher specificity) with different values, so dark mode renders an accidental hybrid palette; three coexisting palettes (indigo dark / red light / flat-blue review-dashboard); ~350 lines of inline `<style>` per auth page with hardcoded indigo rgba; 54 gradients, 16 duplicated keyframes; landing page issues (3 CTA rows, duplicate quick-start sections, `v1.5` badge, `onclick="alert()"` footer links, floating-kana + sparkle animations); emoji-as-icons; no favicon; theme toggle absent on auth pages; no `prefers-color-scheme`.
2. **Design tokens spec** (complete `tokens.css` code block): unified dark "Sumi" + light "Washi" palettes in ONE hue family — vermillion accent `#C1272D` family with AA-safe text variants; warm-neutral surfaces; Inter + Noto Sans JP type scale; 4pt spacing scale; 2-level subtle shadows replacing glow; 150–200ms motion spec.
3. **Theme architecture fix** — single source of truth in `tokens.css` (`body.light-theme` overrides only; delete the conflicting `body.dark-theme` block), `light-theme.css` retired; `prefers-color-scheme` fallback with existing `localStorage.theme` key preserved; one toggle on all 11 pages incl. auth, synced with Settings.
4. **Component restyle specs** — buttons (solid/ghost, no shine sweep, no scale hover), inputs, flat header + sidebar, chat bubbles (tinted, not gradient), modals, notebook tabs (underline indicator), JLPT badges (single-hue tint ramp), review-dashboard converted to tokens, toasts, model selector, landing page restructure (1 hero + 2 CTAs, merge duplicate sections, remove alert() links).
5. **Icon system & brand** — SVG sprite (16 icons), favicon (vermillion rounded square + 日 mark), logo wordmark, emoji policy (SVG for UI chrome only).
6. **Page-by-page edit list** for all 11 HTML files + the 5 CSS files, including: new `auth.css` extracted from inline styles, `<link>` font loading replacing CSS `@import`, dead CSS deletions (broken `.theme-toggle-button`, duplicate `@media` blocks), class typo fixes (`stat-nuber`, `chedule-card`) with JS-reference grep.
7. **Accessibility bar** — WCAG AA contrast pairs enumerated, focus-visible rings, `prefers-reduced-motion`, 44px hit targets.
8. **Phased implementation order** (7 phases, each leaves the app shippable) + QA matrix (11 pages × 2 themes) so the redesign can be executed in a follow-up session.

## Verification
- After agents finish: `ls *.md` root invariant check, `ls -R docs/` structure check, broken-link grep across living docs/scripts, spot-read the new README + CHANGELOG.
- Part 2 doc reviewed against audit evidence (every recommendation cites file:line).
- Final summary to you: docs outcome + redesign plan highlights.