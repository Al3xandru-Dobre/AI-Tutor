Create a project-scoped ZCode skill: **`tutor-dev-guidelines`** — development + security-hardening guidelines for the Japanese AI Tutor, distilled from web research (Node.js best practices, Chroma/vector-DB guidelines, Redis guidelines, OWASP) and mapped onto this project's actual stack.

## Location & structure

```
/home/alex/Projects/Licenta-2027/AI-Tutor-development/.agents/skills/tutor-dev-guidelines/
├── SKILL.md                                 (≤250 lines: always-apply rules + routing)
└── references/
    ├── nodejs-express-security.md           (Express/Node security hardening)
    ├── redis-guidelines.md                  (Redis usage & security)
    └── chroma-vector-guidelines.md          (ChromaDB / vector / RAG)
```

Skill-creator defaults: `.agents/skills/` project path (project-specific skill), progressive disclosure via `references/`.

## SKILL.md contents

- **Frontmatter**: pushy trigger description — fires on any backend/API/auth work, Redis or ChromaDB/RAG/embedding changes, new endpoints, security topics, or `frontend/js/api.js` changes in this repo.
- **Stack map** (one screen): entry `backend/server.js` (middleware order), routes→controllers→services layering, `middlewear/` (sic — do NOT rename casually, 5 files import from it), stores: Redis (users/tokens/rate-limits), AES-256-GCM-encrypted JSON in `backend/data/`, ChromaDB collection `japanese_tutor_knowledge` (port 8000), multi-provider LLMs via `ModelProviderService`. Frontend: vanilla, all fetches through `frontend/js/api.js` (credentials-include + X-CSRF-Token + auto-refresh), `tokens.css` is the only styling-token source.
- **Always-apply security rules** (from OWASP NodeJS cheat sheet, goldbergyoni nodebestpractices, Express production-security guide — each with a one-line *why*): validate every `req.body` via `middlewear/validation.js`; never return `error.message` to clients; secrets only via dotenv; new public endpoints get a Redis-backed limiter from `middlewear/rateLimiter.js`; state-changing endpoints must pass the existing CSRF double-submit and the frontend must use `api.js` helpers; Redis keys follow existing namespaces (`user:`, `token:`, `email:`, `ratelimit:`) and always get TTLs; Chroma embedding model/dimension must match the target collection.
- **Known-landmines register** (verified, with file:line): `cookie-parser` missing from deps + `server.js` stack while `req.cookies` is read in auth/CSRF middleware; Chroma collection deleted on every startup (`enhancedRAGService.js:68`, no env guard); `globalLimiter`/`apiLimiter` defined but never mounted; `csurf` in deps but unused (and deprecated); `conversationController.js:13` leaks `error.message`; chat/notebook/conversation routes lack validation middleware; CSP disabled outside production; live API keys present in `.env` (referenced only — key values never reproduced); project is NOT a git repo → archive, don't delete.
- **Routing table**: which reference file to read per task type; pointers to existing docs (`docs/architecture.md`, `docs/guides/redis.md`, `docs/guides/chromadb.md`, `CONTRIBUTING.md`) instead of duplicating them.

## references/ files

1. **nodejs-express-security.md** (~250 lines) — helmet/CSP that works with a vanilla served frontend; centralized error-handler pattern in project style (CommonJS, 2-space); extending `validation.js` (project pattern, not zod); express-rate-limit with the existing `CustomRedisStore`; JWT-in-httpOnly-cookie rules (already established); explicit `express.json({ limit }) `; logging recommendation (pino); `npm audit` workflow; OWASP Docker notes matching the existing non-root Dockerfile.
2. **redis-guidelines.md** (~200 lines) — key naming + TTL discipline matching existing `RedisService.js`/`UserRedis.js`; never store secrets/plaintext payloads beyond current design; server hardening (bind 127.0.0.1, ACL/password — Redis runs non-containerized per docs); rate-limit-redis pattern; caching patterns for the planned "smart caching" roadmap item; health-check + graceful-shutdown patterns.
3. **chroma-vector-guidelines.md** (~200 lines) — collection lifecycle: get-or-create vs the current delete-and-recreate (env-guard pattern); embedding model/dimension consistency across the 4 embedding services (e5-base / MiniLM / fine-tuned 384-dim BERT); batched upserts; metadata filtering for RAG queries; persistence via the port-8000 server; integration points for planned notebook semantic search (`docs/plans/notebook-roadmap.md`).

Each reference ends with a **Sources** section linking the research (OWASP cheat sheets, nodebestpractices, Express/Node.js official docs, Redis official security docs, AltexSoft Chroma production write-up).

## After writing (skill-creator loop)

Propose 2–3 realistic test prompts, e.g. "Add a DELETE /api/notebook/entries/:id route that also removes the entry from Chroma" and "Cache repeated LLM answers in Redis" — run each in a fresh turn with the skill loaded, review that it triggers and produces guideline-following output, iterate wording until clean.

## Not doing

No code changes, no `.env` edits (bug fixes are follow-up tasks per your choice), no duplication of `docs/history/`, `CHANGELOG.md`, or README content.